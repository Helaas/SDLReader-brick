# TG5040 toolchain container (prebuilt on GHCR)
# This image bundles the cross compiler, sysroot, and supporting libraries for the TrimUI Brick/Smart Pro.
FROM ghcr.io/loveretro/tg5040-toolchain:latest

# Align with docker-compose/Makefile.docker volume mount
WORKDIR /workspace
VOLUME ["/workspace"]

CMD ["/bin/bash"]
