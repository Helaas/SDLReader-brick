# TG5050 toolchain container (prebuilt on GHCR)
# This image bundles the cross compiler, sysroot, and supporting libraries for the TrimUI Smart Pro S.
FROM ghcr.io/loveretro/tg5050-toolchain:latest

# Align with docker-compose/Makefile.docker volume mount
WORKDIR /workspace
VOLUME ["/workspace"]

CMD ["/bin/bash"]
